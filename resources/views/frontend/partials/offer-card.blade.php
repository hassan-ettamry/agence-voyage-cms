@php($cover = $offer->media ?: $offer->destination?->media?->first())
<a href="{{ app(\App\Services\PublicSiteUrl::class)->offer($siteAgency ?? $offer->agency, $offer) }}" class="site-card group flex h-full flex-col overflow-hidden no-underline">
    <div class="relative aspect-[4/3] overflow-hidden bg-slate-100">@if($cover)<img src="{{ $cover->url }}" alt="{{ $cover->alt_text ?? $offer->title }}" loading="lazy" class="h-full w-full object-cover transition duration-500 group-hover:scale-105">@endif @if($offer->is_special)<span class="absolute left-4 top-4 rounded-full bg-[var(--site-accent)] px-3 py-1 text-xs font-bold uppercase tracking-wider text-[var(--site-secondary)]">Special</span>@endif</div>
    <div class="flex flex-1 flex-col p-6">@if($offer->destination)<div class="site-eyebrow">{{ $offer->destination->name }}</div>@endif<h3 class="site-heading mt-2 text-2xl">{{ $offer->title }}</h3><p class="site-copy mt-3 line-clamp-3">{{ $offer->description }}</p><div class="mt-auto flex items-end justify-between gap-4 pt-6"><div><div class="text-xs uppercase tracking-wider text-[var(--site-muted)]">From</div><div class="text-xl font-bold text-[var(--site-primary)]">{{ number_format((float) $offer->price, 2) }}</div></div><span class="font-bold text-[var(--site-secondary)]">{{ $offer->duration_days }} days &rarr;</span></div></div>
</a>
